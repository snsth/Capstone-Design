# 캡스톤 디자인 — 나의 작업 내역 정리 (GODOFPINGU)

> 기간: 2026년 4월 26일 ~ 2026년 6월 22일  
> 담당: 3D BackRoom 파트 전체 (Unity HDRP 17.4.0 / Unity 6)

---

## 1. 작업 개요

나는 이 프로젝트에서 **3D BackRoom 파트** 전체를 담당했다.  
BackRoom은 좁고 어두운 공간에서 몬스터를 피해 열쇠를 찾고 탈출하는 1인칭 공포 게임이다.

---

## 2. 초기 환경 설정 (2026-04-26)

- **HDRP 패키지 설치** — Unity 프로젝트를 URP에서 HDRP 17.4.0으로 전환
- **레벨 디자인 초안** — BackRoom 씬의 기본 공간 배치
- **플레이어 오브젝트 씬 배치** — CharacterController 기반 플레이어 초기 셋업

---

## 3. BackRoom 씬 기초 구현 (2026-06-15)

커밋: `Add BackRoom scene, player, inventory, water systems and pool prefab`

### 3.1 플레이어 컨트롤러 (`BR_PlayerController`)
- WASD 이동 + 마우스 시점(1인칭)
- 달리기(Shift), 점프(Space)
- **헤드 밥(Head Bob)**: 걷는 리듬에 따라 카메라가 자연스럽게 흔들림
- CharacterController 기반 물리 이동

### 3.2 인벤토리 시스템 (`BR_Inventory`)
- 아이템 줍기(E키), 인벤토리 열기(TAB)
- 우클릭 컨텍스트 메뉴로 **장착 / 드롭** 선택
- 손전등 아이템 장착/해제 연동

### 3.3 수중 트리거 영역
- `BR_WaterZone` — 일반 수영 구역 (콜라이더 트리거)
- `BR_OceanZone` — 바다 구역 (자동 침강 + 산소 고갈)
- 수영장 프리팹 제작

---

## 4. 3D 주요 기능 구현 (2026-06-21)

### 4.1 사운드 시스템 (`BR_SoundManager`)

| 사운드 종류 | 구현 내용 |
|---|---|
| 발소리 (Footstep) | 2걸음마다 1회 재생, 여러 클립 중 랜덤 선택 |
| 물 진입음 | 첫 물 접촉 시 1회 |
| 배경음 (BGM) | 씬 시작부터 루프 재생 |
| 수중 앰비언트 | 물속에서만 재생 |
| 형광등 지지직 | 랜덤 간격, 1~3회 연속 버스트 |
| 좀비 비명 | 사망 연출 + 문 열림 후 루프 |

- `BR_SoundManager` 싱글톤으로 씬 전체 사운드 통합 관리
- 배경음/수중음 채널 분리 (이전에 잘못 섞여있던 것 수정)

---

### 4.2 형광등 시스템 (`BR_FluorescentLight`)

- 씬 내 형광등 오브젝트 자동 탐색 (대소문자 구분 없이 이름 탐색)
- **개별 깜빡임**: 형광등마다 독립적인 랜덤 주기로 깜빡임
- **전체 암전**: 3~5초 랜덤 간격으로 모든 불이 동시에 꺼졌다 켜짐
- **Point Light 자동 페어링**: 형광등 오브젝트 하위의 Point Light를 자동으로 함께 제어
- **다중 Renderer 지원**: 형광등 메시가 여러 오브젝트에 분산된 경우에도 정상 동작
- 형광등 지지직 사운드 연동

---

### 4.3 몬스터 AI 시스템 (`BR_Monster`)

#### 기본 추격 AI
- NavMeshAgent 기반 경로 탐색 + 자동 추격
- 추격 범위(`chaseRange`), 감지 범위(`detectionRange`), 킬 거리(`killDistance`) 독립 설정

#### 시야 감지 시스템
- 플레이어가 몬스터를 바라보고 있으면 몬스터가 동결됨
- 3개 체크포인트 (발, 허리, 머리)로 정밀하게 감지
- Raycast로 벽 관통 방지 — 물리적으로 차단된 경우 시야 감지 무효

#### 문 열림 후 추격씬
- 열쇠로 문을 열면 → `alwaysChase = true` → 시야 감지 완전 해제, 무조건 추격
- 새로 스폰되는 몬스터도 플래그 유지 (Start()에서 리셋하지 않도록 처리)

---

### 4.4 몬스터 스폰 시스템 (`BR_MonsterSpawner`)

- NavMesh 위 랜덤 위치에 스폰
- 플레이어와 최소 거리 보장 (너무 가까이 스폰 방지)
- **물속 스폰 방지** — IsInWater() 체크
- **캡슐 장애물 체크** (`Physics.CheckCapsule`) — 좁은 공간, 벽 사이에 끼임 방지
- `Activate()` 메서드 — 문 열릴 때 대량 추가 스폰 트리거

---

### 4.5 문 / 탈출 시스템 (`BR_Door`)

- **열쇠 아이템**으로만 열 수 있는 잠긴 문
- 열릴 때 **슬라이드 애니메이션** (위/아래/옆 방향 설정 가능)
- **탈출 문** 설정 시 열리면 다음 씬으로 전환
- 문 열림 이벤트 → 몬스터 대량 스폰 + 전방위 추격 + 비명 루프 연결

---

### 4.6 수중 / 바다 시스템

- **수영 모드** 자동 전환 (물 트리거 진입 시)
- **자동 침강** (`sinkSpeed`): 바다에서는 가만히 있으면 아래로 가라앉음
- **산소 시스템**:
  - 물속에서 산소 감소, 수면 위로 오면 회복
  - 산소 0% → 씬 재시작 (사망)
- **UI 산소바**: 잠수 시 화면 하단에 O2 퍼센트 표시, 평소에는 숨김
- **지상 점프 복구**: 물 트리거 안에 있어도 바닥에 서 있으면 정상 점프 가능 (버그 수정)
- **언더워터 이펙트**: 물속 진입 시 수중 앰비언트 사운드 재생

---

### 4.7 죽음 연출 시스템 (`BR_Monster.KillPlayer()`)

몬스터에게 잡혔을 때 3단계 연출:

| 단계 | 시간 | 내용 |
|---|---|---|
| Phase 0 | 0.2초 | 슬로우 모션(×0.15) + 폭발적 카메라 흔들림 |
| Phase 1 | 0.4초 | 카메라가 몬스터 얼굴로 빠르게 돌진 + 강한 흔들림 |
| Phase 2 | 1.2초 | 몬스터 얼굴 클로즈업 유지 + 흔들림 서서히 감쇠 |

- 좀비 비명 사운드 재생 (Phase 1 끝 타이밍)
- 카메라 방향 계산: 플레이어→몬스터 벡터 기반 → 항상 얼굴이 정면에 보임
- 연출 완료 → 씬 재시작

---

## 5. 연출 및 완성도 향상 (2026-06-21 ~ 06-22)

### 5.1 HDRP 포스트 프로세싱 (`BR_PostProcessing`)

**평소에는 포스트 프로세싱 완전 비활성** (화면 그대로 유지)  
**사망 순간에만** 아래 효과 순서대로 발동:

| 효과 | 내용 |
|---|---|
| 흰 섬광 | 화면이 순간 하얗게 번쩍임 (0.05초) |
| 핏빛 전환 | 흰색 → 붉은 색 필터로 전환 (0.25초) |
| 색수차 | ChromaticAberration 최대 → 서서히 감소 |
| 렌즈 왜곡 | 화면 왜곡 효과 → 서서히 복귀 |
| 필름 그레인 | 화면 노이즈 (강도 0.65) |

- HDRP Volume을 코드로 런타임에 동적 생성 (씬에 컴포넌트 배치 불필요)

---

### 5.2 시작 자막 (`BR_UIManager`)

- 씬 시작 시 "열쇠를 찾아 비밀문을 열고 탈출하세요." 자막 표시
- 페이드인(0.8초) → 유지(4초) → 페이드아웃(0.8초) 순서로 재생
- IMGUI 기반 렌더링 (Canvas 없음)

---

### 5.3 난이도 조절 (PlayerPrefs)

- **첫 플레이**: 몬스터 속도 15 (매우 빠름 — 한 번은 죽게 됨)
- **사망 후 재시작**: 몬스터 속도 4 (천천히 추격)
- `PlayerPrefs.GetInt("BR_HasDied")` 로 사망 여부 영구 기록 → 씬 재시작 후에도 유지

---

### 5.4 손전등 시스템 (`BR_FlashlightController`)

- 씬에 오브젝트를 미리 배치하지 않아도 됨
- 손전등 아이템 장착 시 플레이어 카메라에 **스포트라이트를 코드로 동적 생성**
- 장착/해제 시 자동으로 켜짐/꺼짐

---

## 6. 버그 수정 내역

| 버그 | 원인 | 수정 방법 |
|---|---|---|
| 점프가 가끔 안 먹힘 | `cc.isGrounded`를 `Move()` 이후에 체크해서 값이 달라짐 | 프레임 시작 시 `bool grounded = cc.isGrounded`로 미리 캐싱 |
| 몬스터 여러 마리가 동시에 죽음 연출 | 여러 몬스터가 동시에 킬 거리 진입 | `static bool anyKilling` 플래그로 첫 번째만 실행 |
| 문 연 후 새 몬스터가 다시 얌전해짐 | 새 몬스터 `Start()`에서 `alwaysChase=false` 리셋 | `alwaysChase` 초기화 코드 제거 (`anyKilling`만 리셋) |
| 물속에 몬스터가 스폰됨 | 스폰 위치 체크 부재 | `IsInWater()` 체크 추가 |
| 좁은 공간에서 몬스터가 끼임 | 스폰 위치에 공간 체크 없음 | `Physics.CheckCapsule()`로 스폰 전 공간 확인 |
| 죽음 연출 시 몬스터 얼굴 안 보임 | `transform.forward`가 실제 시선과 달랐음 | 카메라→머리 벡터를 직접 계산해 방향 결정 |

---

## 7. 작업 타임라인

```
2026-04-26  HDRP 환경 설정, 레벨 디자인 초안, 플레이어 씬 배치

2026-06-15  BackRoom 씬 기초 구현
            ├─ 플레이어 컨트롤러 (이동/시점/점프/헤드밥)
            ├─ 인벤토리 시스템
            └─ 수중/바다 트리거 구역

2026-06-21  3D 핵심 기능 집중 개발 (14개 커밋)
            ├─ 사운드 시스템 통합 (발소리/배경음/수중음/비명)
            ├─ 형광등 깜빡임 + 전체 암전 + 소리 연동
            ├─ 몬스터 AI (시야 감지 / 추격 / 킬 거리)
            ├─ 몬스터 스폰 (물속/좁은공간 방지)
            ├─ 수중/바다 시스템 (침강/산소/UI)
            ├─ 문 슬라이드 + 탈출 시스템
            ├─ 죽음 연출 (3단계 카메라 연출)
            └─ 점프 버그 수정

2026-06-22  완성도 향상 (6개 커밋)
            ├─ HDRP 포스트 프로세싱 (사망 시 섬광/핏빛/색수차)
            ├─ 시작 자막 시스템
            ├─ 난이도 조절 (사망 후 몬스터 속도 감소)
            ├─ 몬스터 스폰 끼임 방지 (캡슐 체크)
            └─ 손전등 스포트라이트 동적 생성
```

---

## 8. 내가 작성한 주요 스크립트

| 스크립트 | 역할 |
|---|---|
| `BR_PlayerController.cs` | 플레이어 이동, 마우스 시점, 헤드밥, 수영, 점프 |
| `BR_Monster.cs` | 몬스터 AI, 시야 감지, 추격, 죽음 연출 카메라 연출 |
| `BR_MonsterSpawner.cs` | 몬스터 스폰 (랜덤 위치, 물속/끼임 방지) |
| `BR_Door.cs` | 열쇠 문, 슬라이드 열림, 탈출 씬 전환 |
| `BR_Inventory.cs` | 아이템 관리, 장착/해제/드롭 |
| `BR_UIManager.cs` | 크로스헤어, 상호작용 프롬프트, 인벤토리, 산소바, 자막 |
| `BR_SoundManager.cs` | 발소리/배경음/수중음/형광등/비명 통합 사운드 관리 |
| `BR_PostProcessing.cs` | HDRP Volume 동적 생성, 사망 연출 포스트 프로세싱 |
| `BR_FlashlightController.cs` | 손전등 스포트라이트 동적 생성 및 ON/OFF 제어 |
| `BR_FluorescentLight.cs` | 형광등 개별 깜빡임, 전체 암전, Point Light 페어링 |
| `BR_WaterZone.cs` / `BR_OceanZone.cs` | 수중 트리거 영역, 수영 모드 전환 |
